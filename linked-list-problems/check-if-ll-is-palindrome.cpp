/*
    1.Traverse the list and push every visited node to a stack
    2.Traverse the list again and for every visited node ,  pop from the stack and compare the value of the traversed and popped value
    3.if all the nodes matched , return true else return false
*/






